import * as React from "react";

interface ISoloMenuProps {}

const SoloMenu: React.FunctionComponent<ISoloMenuProps> = (props) => {
  return (
    <div>
      <p>Each menu goes gere</p>
    </div>
  );
};

export default SoloMenu;
